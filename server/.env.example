# Server environment variables
PORT=4000
NODE_ENV=development

# Database (Neon Postgres)
DATABASE_URL=postgres://USER:PASSWORD@HOST/dbname?sslmode=require

# JWT secrets (generate strong random strings)
JWT_ACCESS_SECRET=replace-with-strong-secret
JWT_REFRESH_SECRET=replace-with-strong-secret

# CORS/client
CLIENT_URL=http://localhost:5173
# COOKIE_DOMAIN can be omitted for localhost. For production, set to your domain (e.g., yourdomain.com)
# COOKIE_DOMAIN=

# Seeded admin account (used by scripts/seed.js)
ADMIN_EMAIL=madolchepuddingcess08@gmail.com
ADMIN_NAME=Elijah June L. Daz
ADMIN_PASSWORD=ejprojectpass_11

# Timezone for calendar and daily quote
TZ=Asia/Singapore
